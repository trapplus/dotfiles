[project]
name = "dotfiles-scripts"
version = "0.1.0"
description = "Dotfiles management scripts for fish, kitty, nvim, and fastfetch"
readme = "README.md"
requires-python = ">=3.9"
dependencies = []

[tool.uv]
package = false
